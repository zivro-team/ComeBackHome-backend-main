server:
  port: 8085
  address: 0.0.0.0 
  forward-headers-strategy: framework

spring:
  profiles:
    include: secret  # 민감정보 secret 파일
   #active: dev  # 개발환경 dev 파일

  logging:
    level:
      project.comebackhomebe.domain: DEBUG
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.jdbc.bind: TRACE
      org.hibernate.orm.jdbc.extract: TRACE
      org.springframework.transaction.interceptor: TRACE
      org.springdoc: DEBUG
      org.springframework: DEBUG

springdoc:
  packages-to-scan: project.comebackhomebe.domain
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: /swagger-ui/index.html
    url: /v3/api-docs
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: alpha

slack:
  webhook-uri: https://hooks.slack.com/services/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
  username: "임현성_백엔드"
  emoji: ""
  channel: ""